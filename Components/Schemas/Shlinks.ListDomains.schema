{
  "$schema": "https://schemas.botframework.com/schemas/component/v1.0/component.schema",
  "$role": "extends(Shlinks.BotComponent)",
  "type": "object",
  "title": "List Domains",
  "description": "Retrieves the list of domains from the API and stores it in resultProperty.",
  "required": [
    "baseUrl",
    "apiKey",
    "resultProperty"
  ]
}
